{
  "name": "@ralphschuler/screeps-wiki-mcp",
  "version": "0.1.0",
  "description": "Model Context Protocol (MCP) server for accessing the Screeps community wiki at wiki.screepspl.us",
  "license": "MIT",
  "author": "OpenAI Automations",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": "./dist/server.js",
  "files": [
    "dist",
    "README.md"
  ],
  "private": true,
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:inspector": "vitest run tests/inspector",
    "inspect": "npx @modelcontextprotocol/inspector node dist/server.js",
    "inspect:cli": "npx @modelcontextprotocol/inspector --cli node dist/server.js",
    "docker:build": "docker build -t screeps-wiki-mcp:latest .",
    "docker:run": "docker run --rm -i -e WIKI_CACHE_TTL screeps-wiki-mcp:latest",
    "docker:push": "docker tag screeps-wiki-mcp:latest ghcr.io/ralphschuler/screeps-wiki-mcp:latest && docker push ghcr.io/ralphschuler/screeps-wiki-mcp:latest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.2",
    "cheerio": "^1.0.0",
    "node-fetch": "^3.3.2",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@modelcontextprotocol/inspector": "^0.18.0",
    "@types/node": "^25.0.9",
    "@vitest/coverage-v8": "^4.0.17",
    "typescript": "^5.4.3",
    "vitest": "^4.0.14"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
